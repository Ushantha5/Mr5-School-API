PORT=
NODE_ENV=
# Replace with your MongoDB connection string. Do NOT commit secrets to source control.
# Example for MongoDB Atlas (URL-encode special chars in password):
# MONGO_URI=mongodb+srv://username:password@cluster0.mongodb.net/mr5school?retryWrites=true&w=majority
MONGO_URI=


# JWT
JWT_SECRET=
JWT_EXPIRE=

# Avathor special token used by the Avathor auto-register endpoint
AVATHOR_SECRET_TOKEN=

# CORS origin for the frontend
CORS_ORIGIN=
